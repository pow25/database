1, build a catalog
2, Create, retrieve, updata, delete json documents in a realteional database. 
3, Break down into sub problems( still need to consider the links between these tables. )
4, some one provides data and schema and you need to manipulate it. And you are provided with the abstract, and 
you need to build up the schema related. (Remember to check whether the coloumn names, fields name, table name
 are correct, and data type value matches or not.)
CSVTable Engine will focus on handling the data of the DB. (join, find), join(nested for loop, for every row
in left table, check the criteria and join/ self-join)

operator push-down(query optimizaiton)